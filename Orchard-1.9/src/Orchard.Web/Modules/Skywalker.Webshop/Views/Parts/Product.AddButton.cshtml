@{
    var productId = (int)Model.ProductId;
}
@using (Html.BeginForm("Add", "ShoppingCart", new { id = productId, area = "Skywalker.Webshop" }))
{
    <button type="submit">@T("Add to Shopping Cart")</button>
}